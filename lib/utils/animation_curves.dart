import 'package:flutter/animation.dart';

class AppCurves {
  static const slowBreath = Cubic(0.4, 0.0, 0.6, 1.0);

  static const floatSmooth = Cubic(0.25, 0.1, 0.25, 1.0);

  static const fadeSlow = Cubic(0.0, 0.0, 0.2, 1.0);

  static const pulseSharp = Cubic(0.7, 0.0, 0.3, 1.0);
}
